@using Dev.JoshBrunton.DynamicsEndpointDiscovery.Rcl.Components.Receivers
@inherits LayoutComponentBase
@inject IFileSystem FileSystem

@Body

<div style="color: #cccccc; position: absolute; bottom: 0; right: 0;">
    <a @onclick="() => Launcher.Default.OpenAsync(FileSystem.AppDataDirectory)">
        @Version
    </a>
</div>

<FluentToastProvider MaxToastCount="1" Timeout="100" />
<FluentDialogProvider />
<FluentTooltipProvider />
<FluentMessageBarProvider />
<FluentMenuProvider />

<ToastMessageReceiver />